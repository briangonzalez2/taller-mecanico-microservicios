server:
  port: 8081

spring:
  application:
    name: users-service

  datasource:
    url: jdbc:mysql://192.168.56.101:3306/users_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: micro
    password: micro123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    

  cloud:
    consul:
      host: 192.168.56.101
      port: 8500
      discovery:
        register: true
        enabled: true
