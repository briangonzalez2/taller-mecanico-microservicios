server:
  port: 8083

spring:
  application:
    name: orderservice

  datasource:
    url: jdbc:mysql://192.168.56.101:3306/orders_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  cloud:
    consul:
      host: 192.168.56.101
      port: 8500
      discovery:
        register: true
        enabled: true
        prefer-ip-address: true
