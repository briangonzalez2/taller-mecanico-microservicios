server:
  port: 8084

spring:
  application:
    name: vehicleservice

  datasource:
    url: jdbc:mysql://192.168.56.101:3306/vehicle_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  cloud:
    consul:
      host: 192.168.56.101
      port: 8500
      discovery:
        register: true
        enabled: true
        prefer-ip-address: true
